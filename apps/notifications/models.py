# Notifications are sent synchronously in the request cycle (see
# ASYNC & SCHEDULED WORK) — there is no queued/stored notification model.
# This module is kept present for structural consistency with the other
# domain apps and as the natural home for one, should a future requirement
# (e.g. in-app notification history) need it.
